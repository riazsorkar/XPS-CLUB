#include<bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long t,a,b;
    cin>>t;
    while(t--){
        long long d = 0;
        cin>>a>>b;
        cout<<(a ^ b)<<endl;
    }
    return 0;
}